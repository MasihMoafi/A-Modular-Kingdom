# Project Context

## Domain Terms

- **MCP Host module**: The module that exposes tools/resources over MCP stdio transport.
- **Web Acquisition module**: The module responsible for query-based URL discovery and HTTP fetch behavior used by web tools.
- **Memory module**: The module family handling scoped memory persistence and retrieval.
- **Memory Path Policy module**: The module that defines the single runtime policy for memory base-path resolution.
- **RAG module**: The module family handling document indexing and retrieval pipelines.
